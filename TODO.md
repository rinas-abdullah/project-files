# TODO
- [x] Update next.config.ts for static export compatibility by setting `images: { unoptimized: true }` while keeping `output: 'export'`.
- [x] Rebuild/export to confirm the Image Optimization runtime error is resolved.


